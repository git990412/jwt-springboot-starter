server:
  port: 8090
  address: localhost
spring:
  datasource:
    url: jdbc:h2:./medium;MODE=MYSQL;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      settings:
        web-allow-others: true